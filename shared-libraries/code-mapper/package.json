{
  "name": "@truevow/code-mapper",
  "version": "1.0.0",
  "description": "Scans service repos, generates structure maps with dependency graphs, flags dead code and hygiene issues",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "map": "tsx src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0"
  },
  "dependencies": {}
}
